# 설정

기본 파일: 저장소 루트 `config/config.json`  
앱은 리포 루트를 기준으로 이 경로를 후보로 로드합니다.

보조: `config/hsv_settings.json` (HSV 피커)

## 최상위 구조

```json
{
  "production_system": { ... },
  "vision_algorithms": { ... },
  "analytics": { ... },
  "gui": { ... },
  "performance": { ... },
  "compatibility": { ... },
  "concealment": { ... }
}
```

## `production_system`

| 필드 | 의미 |
|------|------|
| `name` | 제품 표시 이름 계열 |
| `version` | 설정 스키마/제품 버전 문자열 |
| `purpose` | 용도 설명 |
| `mode` | 예: `production_mode` |

## `vision_algorithms`

| 필드 | 의미 |
|------|------|
| `selected_algorithm` | 예: `"yolo26"` |

### `hsv_tracking`

| 필드 | 의미 |
|------|------|
| `enabled` | HSV 파이프라인 on/off |
| `lower_bound` / `upper_bound` | HSV 하한·상한 `[H,S,V]` |
| `morphology_kernel_size` | morphology 커널 |
| `min_contour_area` | 최소 contour 면적 |

### `yolo26_detection`

| 필드 | 의미 |
|------|------|
| `enabled` | YOLO 추론 on/off |
| `onnx_model_path` | 기본 `models/yolo26n.onnx` |
| `class_names_path` | 기본 `models/coco_classes.txt` |
| `confidence_threshold` | 신뢰도 임계값 |
| `max_detections` | 최대 검출 수 |
| `input_size` | `[width, height]` — 권장 `[640, 640]` |
| `execution_providers` | 예: `["directml","openvino","cpu"]` 또는 `"auto"` |
| `selected_gpu_id` | DirectML 장치 ID |
| `openvino_device_type` | `null` 또는 `"GPU"`, `"NPU"`, `"GPU.0"` 등 |

`"auto"` 또는 빈 목록은 기본 순서(DirectML → OpenVINO → CPU)로 확장됩니다.

## `analytics`

| 필드 | 의미 |
|------|------|
| `enabled` | 분석/이력 스위치 (일부 필드는 추후 연동) |
| `max_history_size` | 이력 상한 |
| `fps_calculation_window_sec` | FPS 윈도우(초) |

## `gui`

| 필드 | 의미 |
|------|------|
| `show_performance_metrics` | 성능 지표 표시 |
| `ui_scale` | UI 스케일 |
| `show_metrics_overlay` | 오버레이 표시 |

## `performance`

| 필드 | 의미 |
|------|------|
| `target_fps` | 목표 FPS |
| `enable_multithreading` | 멀티스레드 처리 |
| `max_processing_threads` | 처리 스레드 상한 |
| `frame_buffer_size` | 런타임 `buffer_depth` 로 매핑 |
| `enable_gpu_acceleration` | GPU 가속 의도 플래그 |

## `compatibility`

| 필드 | 의미 |
|------|------|
| `no_hook` | 후킹 없는 경로 선호 |
| `obs_adapter_allowed` | OBS 어댑터 허용(향후/정책) |

## `concealment`

화면 공유 톤·저노출 옵션.

| 필드 | 의미 |
|------|------|
| `enabled` | 마스터 스위치 |
| `blend_in_screen_share` | 일반 공유 앱 톤 |
| `exclude_windows_from_capture` | 자체 창 캡처 제외 |
| `quiet_logs` | 로그 quiet |
| `display_name` / `window_title` / `monitor_title` | UI 문자열 오버라이드 |

## UI에서 저장

운영자 UI의 설정 저장은 로드한 `config.json` 경로로 다시 씁니다.  
HSV 피커 보조 값은 `hsv_settings.json` 쪽에 둘 수 있습니다.

## 관련 페이지

- [시작하기](시작하기)
- [모델-가이드](모델-가이드)
